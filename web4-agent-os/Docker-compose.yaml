version: "3.9"

services:

  agent-runtime:
    build: ./services/agent-runtime
    ports:
      - "3000:3000"

  blockchain-gateway:
    build: ./services/blockchain-gateway
    ports:
      - "4000:4000"

  rust-executor:
    build: ./executor/rust-executor
    ports:
      - "8080:8080"
